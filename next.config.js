/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Using the App Router (default). Ensure no conflicting pages/ routes exist.
}

module.exports = nextConfig;
